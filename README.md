# first-pr-practice

A small practice repository for learning the pull request workflow with Claude Code.

## What this is

This repo exists to try out the full loop: make a change on a branch, push it,
and open a pull request on GitHub.

## Contents

- `.gitignore` — ignores common OS and log file clutter (`.DS_Store`, `Thumbs.db`, `*.log`).
